# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""Fields required to create a new blog post"""
input CreatePostInput {
  """Optional tags for the post"""
  tags: [String!] = []

  """Title of the post"""
  title: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Mutation {
  """
  Creates a new blog post for the currently authenticated user. The author is resolved from the Bearer JWT — no authorId argument needed. The post is created as a draft (isPublished: false) by default. Requires a valid JWT.
  """
  createPost(input: CreatePostInput!): PostModel!

  """
  Deactivates the account of the currently authenticated user. The user is identified from the Bearer JWT — no UUID argument needed. Requires a valid JWT.
  """
  deactivateUser: UserModel!

  """
  Updates the profile of the currently authenticated user (fName, lName). The user is identified from the Bearer JWT — you cannot update another user. Requires a valid JWT.
  """
  updateUser(input: UpdateUserInput!): UserModel!
}

"""Represents a blog post"""
type PostModel {
  """Author's MongoDB ObjectId"""
  authorId: String!

  """When the post was created"""
  createdAt: DateTime!

  """MongoDB ObjectId"""
  id: ID!

  """Whether the post is soft-deleted"""
  isDeleted: Boolean!

  """Whether the post is published"""
  isPublished: Boolean!

  """Tags associated with the post"""
  tags: [String!]!

  """Post title"""
  title: String!

  """When the post was last updated"""
  updatedAt: DateTime!
}

type Query {
  """Returns the profile of the logged-in user. Requires a valid JWT."""
  getProfile: UserModel

  """
  Returns the profile of the user with the given ID. Requires a valid JWT.
  """
  getUserById(uuid: String!): UserModel

  """Returns all active users. Requires a valid JWT."""
  getUsers: [UserModel!]!
}

"""The role of the user in the system"""
enum Role {
  admin
  user
}

"""Fields that can be updated on the current user profile"""
input UpdateUserInput {
  """New first name"""
  fName: String

  """New last name"""
  lName: String
}

"""Represents a user in the system"""
type UserModel {
  """When the account was created"""
  createdAt: DateTime!

  """Email address"""
  email: String!

  """First name"""
  fName: String!

  """MongoDB ObjectId"""
  id: ID!

  """Whether the account is active"""
  isActive: Boolean!

  """Last name"""
  lName: String!

  """Timestamp of the last login"""
  lastLogin: DateTime

  """User role: admin or user"""
  role: Role!

  """Stable public UUID for the user"""
  uuid: String!
}